import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ['fakestoreapi.com'], // Add the domain of your image
  },
};
export default nextConfig;
